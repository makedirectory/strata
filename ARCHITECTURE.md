# Strata — Architecture

The architecture & engineering documentation now lives in the docs site under
`src/content/architecture/` and is served at **`/docs/architecture`**.

Run `npm run dev` and open http://localhost:3000/docs to read it (registry & schema,
domain model, visual mapping, persistence, MCP & IaC import, testing, and roadmap).
